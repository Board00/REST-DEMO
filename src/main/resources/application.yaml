spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sniper_mastery?useSSL=false
    username: root
    password: "@boardKoni1234"

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  #JPA Settings
  jpa.hibernate.ddl-auto: create